# JVM — mémoire suffisante pour AGP en CI
org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8

# AndroidX — requis par core-ktx / appcompat / material / webkit / lifecycle
android.useAndroidX=true

kotlin.code.style=official
